# 🤖 Настройка AI Backend для Виртуального Педагога

## 🎯 Цель
Подключить OpenAI GPT для генерации ответов студентов в симуляторе.

---

## 📋 Вариант 1: Локальное тестирование (Рекомендуется)

### **Шаг 1: Получите OpenAI API Key**

1. Перейдите на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите **"Create new secret key"**
4. Скопируйте ключ (выглядит как `sk-proj-...`)

### **Шаг 2: Добавьте ключ в `.env`**

Откройте файл `.env` в корне проекта и замените:

```bash
OPENAI_API_KEY=your_openai_api_key_here
```

На ваш реальный ключ:

```bash
OPENAI_API_KEY=sk-proj-ABC123...
```

### **Шаг 3: Установите зависимости**

```bash
cd concept-site
npm install
```

### **Шаг 4: Запустите Vercel Dev сервер**

```bash
npx vercel dev
```

Или если у вас установлен Vercel CLI глобально:

```bash
vercel dev
```

### **Шаг 5: Откройте симулятор**

Перейдите на `http://localhost:3000/simulator_v4_avatar.html`

✅ **Теперь AI должен работать!** Студенты будут отвечать через OpenAI GPT-4o-mini.

---

## 🌐 Вариант 2: Деплой на Vercel (Production)

### **Шаг 1: Установите Vercel CLI** (если еще не установлен)

```bash
npm install -g vercel
```

### **Шаг 2: Залогиньтесь**

```bash
vercel login
```

### **Шаг 3: Добавьте environment variable**

```bash
vercel env add OPENAI_API_KEY
```

Когда спросит:
- **Environment:** Выберите `Production`, `Preview`, `Development` (или все)
- **Value:** Вставьте ваш API key `sk-proj-...`

### **Шаг 4: Задеплойте проект**

```bash
vercel --prod
```

### **Шаг 5: Откройте ваш сайт**

Vercel выдаст URL типа `https://your-project.vercel.app`

✅ **AI работает на продакшене!**

---

## ⚙️ Альтернатива: Использовать другие AI провайдеры

### **Google Gemini** (Бесплатный)

1. Получите ключ на [ai.google.dev](https://ai.google.dev)
2. Модифицируйте `api/chat.js`:

```javascript
// Замените OpenAI на Google Generative AI
const { GoogleGenerativeAI } = require("@google/generative-ai");
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);

const model = genAI.getGenerativeModel({ model: "gemini-pro" });
const result = await model.generateContent(messages[0].content);
const content = result.response.text();
```

### **Anthropic Claude**

```javascript
const Anthropic = require('@anthropic-ai/sdk');
const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

const message = await anthropic.messages.create({
  model: "claude-3-haiku-20240307",
  max_tokens: max_tokens,
  messages: messages
});
```

---

## 🐛 Troubleshooting

### **Ошибка: "Failed to fetch"**

✅ **Решение:** Убедитесь что Vercel Dev запущен (`vercel dev`)

### **Ошибка: "Invalid API Key"**

✅ **Решение:**
1. Проверьте что ключ правильно скопирован в `.env`
2. Ключ должен начинаться с `sk-proj-` или `sk-`
3. Перезапустите `vercel dev`

### **Ошибка: "Rate limit exceeded"**

✅ **Решение:**
1. Пополните баланс на [platform.openai.com/billing](https://platform.openai.com/billing)
2. OpenAI требует минимум $5 на счету

### **Студенты отвечают "Хорошо..." (Fallback)**

✅ **Причина:** AI backend не подключен или API key неверный

✅ **Решение:** Проверьте консоль браузера (F12) → вкладка Console

---

## 💰 Стоимость

**OpenAI GPT-4o-mini:**
- ~$0.15 за 1M входных токенов
- ~$0.60 за 1M выходных токенов

**Средний урок (10 реплик студентов):**
- ~2000 токенов = **$0.001** (~0.1 руб)

**1000 уроков = $1** (очень дешево!)

---

## 📊 Мониторинг использования

Проверяйте расход на [platform.openai.com/usage](https://platform.openai.com/usage)

---

## ✅ Проверка что всё работает

1. Откройте симулятор
2. Выберите студентов
3. Напишите: "Здравствуйте, класс!"
4. **Если студент отвечает разнообразно** (не просто "Хорошо...") → ✅ AI работает!
5. **Если "Хорошо..."** → ❌ Проверьте API key и консоль

---

Готово! 🎉
